[pytest]
python_files = test_*.py
python_classes = Test*
python_functions = test_*
[pytest]
markers =
    smoke: Basic high-priority tests to verify core application health
    regression: Full feature tests to verify existing functionality after changes
    critical: Business-critical tests that must pass before release
    destructive: Tests that delete, remove, cancel, or modify existing data